spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/nareum_db?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: nareumadmin! # ???? ?? ??
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000

  #  jpa:
  #    show-sql: true
  #    properties:
  #      hibernate:
  #        format_sql: true
  #        dialect: org.hibernate.dialect.MariaDBDialect
  #        default_batch_fetch_size: 100 # ?? ????

  mybatis:
    mapper-locations: classpath*:mapper/**/*.xml
    type-aliases-package: com.nareumserver.domain
    configuration:
      jdbc-type-for-null: VARCHAR  # ?? ??? mybatis? null ?? ?? ? ??
      map-underscore-to-camel-case: true
      default-enum-type-handler: org.apache.ibatis.type.EnumTypeHandler

logging:
  level:
    # MyBatis ?? ??? (?? ???? ?? ??)
    com.nareumadmin.mapper: DEBUG

    # MyBatis ?? ??
    org.apache.ibatis: DEBUG

    # JDBC ??
    java.sql: DEBUG


server:
  port: 8082